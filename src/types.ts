// src/types.ts

// TODO: Step 2 - สร้าง Interface สำหรับ Product
// กำหนดให้มี id (number), name (string), price (number), และ quantity (number)
export interface Product {
  id: number;
  name: string;
  price: number;
  quantity: number;
}
